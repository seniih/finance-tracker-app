import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const Color primary = Color(0xFF61BC45);

  // Primary Scale
  static const Color primaryHover = Color(0xFF4FA63A);
  static const Color primaryPressed = Color(0xFF3E8530);

  // Background system
  static const Color background = Color(0xFFF8F9FA);
  static const Color surface = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF6B7280);

  // Semantic colors
  static const Color success = Color(0xFF16A34A);
  static const Color error = Color(0xFFDC2626);
  static const Color warning = Color(0xFFF59E0B);
  static const Color info = Color(0xFF0EA5E9);

  // Borders / UI
  static const Color border = Color(0xFFE5E7EB);
  static const Color divider = Color(0xFFE5E7EB);

  // Sidebar
  static const Color sidebarText = Colors.white;
  static const Color sidebarTextMuted = Colors.white70;
  static const Color sidebarTextSubtle = Colors.white38;
  static const Color sidebarDivider = Colors.white24;
  static const Color sidebarDot = Colors.white54;
  static const Color sidebarBalancePositive = Color(0xFFA7F3D0);
  static const Color sidebarBalanceNegative = Color(0xFFFCA5A5);
}